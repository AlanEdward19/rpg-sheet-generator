apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheet-manager-service.fullname" . }}
  labels:
    {{- include "sheet-manager-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheet-manager-service.selectorLabels" . | nindent 4 }}
