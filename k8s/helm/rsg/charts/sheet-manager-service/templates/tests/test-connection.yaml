apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sheet-manager-service.fullname" . }}-test-connection"
  labels:
    {{- include "sheet-manager-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sheet-manager-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
